// Generated by CoffeeScript 1.12.6
(function() {
  var Newnotes;

  Newnotes = require('./newnotes');

  describe('Newnotes', function() {
    return it('creates, then lists a basic todo', function() {
      var newnotes, todo;
      newnotes = new Newnotes;
      newnotes.add('do first');
      newnotes.add('do after');
      return expect([
        (function() {
          var i, len, ref, results;
          ref = newnotes.list();
          results = [];
          for (i = 0, len = ref.length; i < len; i++) {
            todo = ref[i];
            results.push(todo.title);
          }
          return results;
        })()
      ].join(',')).toEqual('do first,do after');
    });
  });

}).call(this);
